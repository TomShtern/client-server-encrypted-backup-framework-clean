version: 2.1

jobs:
  one_click_build_and_run:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: "build and run"
          command: "one_click_build_and_run.py"

workflows:
  say-hello-workflow:
    jobs:
      - one_click_build_and_run