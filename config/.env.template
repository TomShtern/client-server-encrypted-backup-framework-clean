# CyberBackup 3.0 Environment Configuration Template
# Copy this file to .env and fill in the values as needed

# GitHub Integration (Optional - for backup to GitHub repositories) sound idiotic. probably some garbage made up feature.
GITHUB_PERSONAL_ACCESS_TOKEN=your_github_token_here

# Server Configuration (Optional - for API authentication), this is also trash, the server does not have.
SERVER_API_KEY=your_api_key_here

# Database Configuration (Optional - for database encryption)
DATABASE_PASSWORD=your_database_password_here

# Real Server URL (Optional - override mock mode) - THERE IS NO MOCK MODE!
REAL_SERVER_URL=http://localhost:1256

# Development Settings
# FLET_GUI_ONLY_MODE=1  # Set to 1 to run GUI without server. never do gui only mode unless asked.
# FLET_V2_DEBUG=1        # Set to 1 for verbose debug output
# CYBERBACKUP_DISABLE_INTEGRATED_GUI=1  # Disable embedded server GUI. not sure what this is, sound stupid.